"use client";

import AccountEmptyState from "./AccountEmptyState";

const Messages = () => {
  return <AccountEmptyState message="Once you place an order you'll have full access to send messages from this page." />;
};

export default Messages;
